# Executed validation — Recursive Reiteration Engine v0.4.1

The standalone rename retains the runtime; it does not rerun the mathematical
research campaigns or claim new model results. On Python 3.13.5, all **271
unittest methods passed**: 263 inherited methods and eight release-identity checks.
The new checks cover the skill slug, distribution identity, console entry points,
compatibility imports, publication target, source allowlist, and retained attribution.

All **25 existing runtime modules and six Lean-directory files are byte-identical**
to the verified v0.4.0 input. The workspace, progress-memory, rational-refinement,
and folding examples ran successfully. They are deterministic local examples,
not neural-model benchmarks. The offline wheel build succeeded with the installed
build tools and no dependency downloads.

Publication tests use mocked remote commands. The actual publication attempt in
the preparation environment stopped for lack of an authenticated GitHub CLI. A
passing test or a local Git root is not evidence of a remote GitHub repository.
The accompanying validation receipt records the fresh-archive and installed-command
checks independently of the historical tests below.

## Historical validation record

The following v0.4.0 and earlier measurements are retained as provenance. They were
not newly reproduced by this identity-only change.

# Executed validation — v0.4.0

## Folding tests

The complete suite now contains **263 unittest methods**, with no skips:
213 inherited v0.3.0 methods and 50 new folding methods. Those include actual
paired evaluator execution, full four-arm composition, finite/formal separation,
strict NaN/type rejection, exact artifact binding, transactional one-use holdout,
crash/reopen behavior, generation sealing, source capabilities, whole-entry memory
selection, rollback, altered interaction receipts and real-controller score-1
false-halt prevention. Some methods contain multiple subcases.

The arithmetic example executes all four evaluation gates and an empirical
promotion. It is a constructed test, not evidence about real-model performance.
The release receipt records fresh ZIP and bundle-clone tests, export hashes,
offline wheel installation, the new example and existing workspace example.

## Test with the existing mathematical research

The separate `pnp-folding-integration` package imports the exact prior 99,783-character
research history, with its 123 finite result identities. Before this integration,
all 100 campaign cases and 23 earlier computations were reexecuted. Import receipts
bind those finite identities; they do not formally verify the historical prose.

A predeclared paired solver experiment uses 32 development, 128 validation and
160 confirmation inputs. Its loss is nondecision within one visited-region cap.
The candidate enables an existing relative-hyperplane recognizer, not a new
algorithm invented for this release. A deliberately unsound interpretation of
missing certificates is killed by the separately implemented proof checker.
Both arms run as local subprocesses with hard per-process timeouts; no neural model
or inference API is invoked.

The first recorded confirmation produces 30/160 checked decisions for the baseline
and 107/160 for the enabled recognizer, with zero resource/checker failures in those
arms. The predeclared numeric bound passes. Because the bank is a constructed,
deduplicated structural mixture, this is **operational selection on that suite**,
not a proven statistical guarantee for arbitrary independent SAT instances or a
wall-time improvement. The full assumptions are in `references/folding.md`.

Four externally checked admissions pass through both full-context and workspace
controller paths. Both retain all prior evidence, 128 progress locks including the
mandatory envelope, and byte-identical complete judge requests. A deliberate
judge score of 1 does not halt the research as solved. Two actual-controller
negative controls reject deletion/forgery and roll back paired answer/notes.

Recorded input is 2,046,508 versus 676,090 Unicode characters, **66.96% less**,
with 17 exchanges in each arm. This is an authored-response transport test;
it is not a tokenizer, billing, fresh neural-reasoning or quality measurement.
The report records exact claims, receipts, replay audits and reproduction limits.

## Prior workspace validation (retained historical record)

# v0.3.0 historical checks

Python 3.13.5. Python 3.10+ is declared, but other versions were not executed.

## Tests and interfaces

**213 unittest methods pass, no skips: 174 pre-existing and 39 new workspace
methods.** Some methods contain many subcases, including 200 seeded Unicode
patch reconstructions. The suite exercises callable, real local subprocess,
manual-parser and CLI paths.

New checks cover total visible-input bounds, exact excerpts and source retrieval,
read-authorized deltas, source hashes, stale/cross-scope/request tickets, malformed
JSON/extra authority fields, edits to unseen text, overlap and type checks, required
passages and transitive dependency closure, oversized/truncated patches, checkpoint
policy/resume, call caps, full validators and judges, all 123 obligation callbacks,
and paired rollback after a rejected proposal. The full judge is deliberately
unbounded by the workspace limit but still subject to the global input limit.

```sh
PYTHONPATH=src python -S -m unittest discover -s tests -v
python -S examples/workspace_demo.py
```

The deterministic workspace demo completes three tiers and preserves the full
historical prefix. It is not a neural inference benchmark. The older Headroom
adapter tests are contract fixtures, not a live upstream-library installation.

## Paired recorded-campaign replay

```sh
python -S benchmarks/replay_campaign.py \
  /path/to/extracted/p-vs-np-100-tier/artifacts/run --out /tmp/workspace-comparison
```

The supplied 100-tier archive is an external test input; no copy of its research
corpus is bundled in this clean source export. Both arms use the same authored
outputs, task, exact pins, full judges and record checks. The script separately
identifies its one-time JSON-format normalization of the seed and checks its
parsed-value identity. It verifies identical full final text, 100 accepted stages,
123 locks, and byte-identical whole-judge requests. Old mathematical computations
are NOT reexecuted; checks validate recorded content/hashes and preservation.

Measured all-input characters: **46,897,048 -> 14,750,161 (68.5478% less)**.
All output characters: **7,706,485 -> 275,617**. Including actual input plus output,
the reduction is **72.4820%**. Calls increased **501 -> 683**, including **182**
archive exchanges. No workspace request exceeded 10,000 characters. Judges retain
9,323,435 input characters in each arm. Read `references/workspace.md` for the
bounded test scope, initial fixture-diff failure, and non-inference qualifications.

## Build and release

The final validation receipt records fresh-extraction tests, paired replay, the
source manifest, offline wheel build and isolated import. The export contains only
explicitly allowlisted source/docs/examples/tests; historical research traces and
the estimator-folding corpus are not included. Files are content hashed, not signed.

No remote publication, real neural-model evaluation, current upstream Headroom
installation, proof-assistant build, formal security proof, semantic dependency
inference, or entire-codebase security audit is claimed. These tests do not measure
model-token usage, billing, or the quality effect of reducing model context.
